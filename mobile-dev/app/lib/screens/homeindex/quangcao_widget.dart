import 'package:flutter/material.dart';

class PromotionPageScreen extends StatelessWidget {
  const PromotionPageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Khuyến mãi & Ưu đãi')),
      body: const Center(child: Text('Giao diện Khuyến mãi')),
    );
  }
}